mixin icon(name, className)
  - var icon = require(`images/icons/${name}.svg`);
  svg(class=className viewBox=icon.viewBox preserveAspectRatio="none")
    use(xlink:href=icon.url)

mixin image(path, className)
  img(src=require(`images/${path}`) class=className)

mixin nav(className, classNameTwo)
  - var section = [{"text": 'Главная', 'link': "#main"}, {"text": 'Обо мне', 'link': "#about"}, {"text": 'Навыки', 'link': "#skills"}, {"text": 'Работы', 'link': "#works"}, {"text": 'Отзывы', 'link': "#reviews"}, {"text": 'Связаться', 'link': "#mail"}]
    nav(class=`${className} ${classNameTwo}`)        
      ul(class=`${className}__list`)            
        each sec in section
          li(class=`${className}__item`)
            a(class=`${className}__link` href=sec.link)= sec.text

mixin menu(className, classNameTwo)
  - var section = [{"text": 'Главная', 'link': "#main"}, {"text": 'Обо мне', 'link': "#about"}, {"text": 'Навыки', 'link': "#skills"}, {"text": 'Работы', 'link': "#works"}, {"text": 'Отзывы', 'link': "#reviews"}, {"text": 'Связаться', 'link': "#mail"}]
    nav(class=`${className} ${classNameTwo}`)        
      ul(class=`${className}__wrapper`)            
        each sec in section
          li(class=`${className}__item`)
            a(class=`${className}__link` href=sec.link)= sec.text

            
  